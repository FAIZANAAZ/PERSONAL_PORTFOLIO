import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false,  // Ye line ko add karen, agar aapko react strict mode ko disable karna hai
  // Aapki other configurations yahaan aa sakti hain
};

export default nextConfig;
